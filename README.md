# Vorogaze

Vorogaze is packaged as a Shiny app that can be installed from GitHub.

```r
pak::pak("mjgreen/vorogazeaoi3")
vorogazeaoi3::run_app()
```

For a branch build, install that branch explicitly:

```r
pak::pak("mjgreen/vorogazeaoi3@package-structure")
vorogazeaoi3::run_app()
```
